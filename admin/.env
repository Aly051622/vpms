use Dotenv\Dotenv;

// Point to the correct directory where your .env file is located
$dotenv = Dotenv::createImmutable('C:/xampp/htdocs/vpms/admin');  // Absolute path to your .env directory
$dotenv->load();

// Fetch the encryption key from the .env file
$encryptionKey = getenv('ENCRYPTION_KEY');

if (!$encryptionKey) {
    die("Encryption key not set in environment variables.");
}
